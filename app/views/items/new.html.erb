<h1>Create an Item</h1>
<%= form_for(@item, :url => item_path(@restaurant.slug, @item), :method => :post, html: {multipart: true}) do |f| %>
  <%= render partial: 'form', :locals => {:f => f} %>
<% end %>
