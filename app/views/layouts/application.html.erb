<!DOCTYPE html>
<html>
 <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Hunger Gains" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Hunger Gains" %>">
     <link href='http://fonts.googleapis.com/css?family=Merriweather+Sans:400,300,700,400italic' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag "application", :media => "all" %>
   <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header class="navbar navbar-fixed-top">
      <div id="logo_image"><a href="/" ><img src="/images/hunger_gains_110.png"></a></div>
      <nav class="navbar-inner">
          <a href="/" class="logo"><img src="/images/hunger_gains.png"></a>
          <div class="controls">
            <% if cookies[:order_id] %>
              <%= link_to "My Order", order_path(cookies[:order_id]), {:class => "btn btn-success"} %>
            <% end %>
            <% if current_user %>
             <%= link_to "My Account", user_path(current_user), {:class => "btn btn-info"} %>
              <%= link_to "Logout", logout_path, {:class => "btn btn-warning"} %>
            <% else %>
              <%= link_to "Login", login_path, {:class => "btn btn-primary login"} %>
            <% end %>
          </div>
        <div class="container">
        </div>
      </nav>
    </header>
    <main role="main">
      <div class="container">
        <div class="content">
           <div class="row">
            <div class="span12">
              <%= render 'layouts/messages' %>
              <%= yield %>
            </div>
          </div>
          <footer>
            <div class="container foot">
              <p>Hunger Gains.</p>
              <p> May the Odds be Ever in Your Flavor.</p>
              <p>Darryl Pequeen, Rolen Le, &amp; Lauren Spath Luhring</p>
            </div>
          </footer>
        </div>
      </div>
    </main>
    <script>
    var easter_egg = new Konami(function overlay() {
  el = document.getElementById("overlay");
  el.style.visibility = (el.style.visibility == "visible") ? "hidden" : "visible";
});
  </script>
</body>
<div id="overlay">
     <div>
          <img src="http://static02.mediaite.com/styleite/uploads/2013/08/tumblr_mhah9xLpRn1riyni7o1_500.gif">
     </div>
</div>
</html>
